- Driver source files available inside /driver folder.
- Giving make command inside /driver folder builds all the source file and creates the library inside /libdriver folder.
- /include folder contains the header declartion of driver source API's
- /application folder contains the application file which calls the driver APIs. Giving Make command in /application folder build the application.

